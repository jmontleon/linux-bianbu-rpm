From 9b874226f7e016eed613d2fba8df6380fea01156 Mon Sep 17 00:00:00 2001
From: lijuan <juan.li@spacemit.com>
Date: Mon, 3 Jun 2024 10:41:50 +0800
Subject: [PATCH 0709/1239] k1: usb: goto valid identifier

Signed-off-by: lijuan <juan.li@spacemit.com>
Change-Id: I528ec7db161cdef545cb1910874cb945fb23c6ed
---
 drivers/usb/dwc3/core.c | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/drivers/usb/dwc3/core.c b/drivers/usb/dwc3/core.c
index d6b26772bd32..b23972eaa7e7 100644
--- a/drivers/usb/dwc3/core.c
+++ b/drivers/usb/dwc3/core.c
@@ -2268,7 +2268,7 @@ int dwc3_core_probe(const struct dwc3_probe_data *data)
 		IS_ENABLED(CONFIG_SOC_SPACEMIT_K1PRO)) {
 		ret = dma_set_mask_and_coherent(dwc->sysdev, DMA_BIT_MASK(40));
 		if (ret)
-			goto disable_clks;
+			goto err_disable_clks;
 	}
 
 	/*
@@ -2288,7 +2288,7 @@ int dwc3_core_probe(const struct dwc3_probe_data *data)
 	if (IS_ENABLED(CONFIG_SOC_SPACEMIT_K1X)) {
 		ret = dma_set_mask_and_coherent(dwc->sysdev, DMA_BIT_MASK(32));
 		if (ret)
-			goto disable_clks;
+			goto err_disable_clks;
 	}
 
 	spin_lock_init(&dwc->lock);
-- 
2.54.0

